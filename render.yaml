services:
  - type: web
    name: servidumbres-grenergy
    env: node
    region: frankfurt
    branch: main
    rootDir: backend
    buildCommand: npm install && cd ../frontend && npm install && npm run build && cp -r dist ../backend/public || cp -r dist ./public
    startCommand: node index.js
    plan: free
